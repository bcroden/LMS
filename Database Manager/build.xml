<?xml version="1.0" encoding="UTF-8" standalone="no"?>

<!-- Auto generated by Eclipse on 2/28/15. Trimmed by Alex -->

<project basedir="." default="build" name="Database Manager">
    
	<!-- Get access to JAVA_HOME etc -->
	<property environment="env"/>
    	
	<!-- Java versions for source code and binaries -->
    <property name="target" value="1.8"/>
    <property name="source" value="1.8"/>
	
	<!-- Key directories -->
	<property name="src.dir" value="src"/>
	<property name="bin.dir" value="bin"/>
	
	<!-- Binary files for executing application  -->
    <path id="Database Manager.classpath">
        <pathelement location="bin"/>
    </path>
    
	<!-- Setup needed directories and copy necessary files -->
	<target name="init" depends="clean">
        <mkdir dir="${bin.dir}"/>
        <copy includeemptydirs="false" todir="${bin.dir}">
            <fileset dir="${src.dir}">
                <exclude name="**/*.launch"/>
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
    </target>
    
	<!-- Delete all non-source files -->
	<target name="clean">
        <delete dir="bin"/>
    </target>
    
	<!-- Compile all source files -->
	<target depends="init" name="build">
        <javac destdir="${bin.dir}" includeantruntime="false" source="${source}" target="${target}">
            <src path="${src.dir}"/>
            <classpath refid="Database Manager.classpath"/>
        </javac>
    </target>
        
	<!-- Execute Database Manager application -->
	<target name="Main">
        <java classname="com.team1.Main" failonerror="true" fork="yes">
            <classpath refid="Database Manager.classpath"/>
        </java>
    </target>
	
</project>
